RewriteEngine On

# Se o arquivo ou diretório solicitado existir, serve diretamente (CSS, JS, imagens)
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

# Tudo o mais redireciona para index.php
RewriteRule ^ index.php [QSA,L]